<?xml version="1.0" encoding="utf-8"?>
<!--
  The demo backend is commonly reached over plain HTTP on a LAN / emulator
  loopback (uvicorn has no TLS by default). Cleartext is therefore permitted
  for local addresses only; everything else still requires HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
